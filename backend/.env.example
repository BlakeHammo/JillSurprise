PORT=3001
ADMIN_PASSWORD=your-secret-password-here
FRONTEND_URL=http://localhost:5173

# ── Persistent storage (production only) ─────────────────────────────────────

# Cloudinary — upload photos/videos to the cloud instead of local disk.
# Get this from your Cloudinary dashboard (Settings → Access Keys → CLOUDINARY_URL).
# Leave empty in development to use local /uploads folder.
CLOUDINARY_URL=

# Database path — point to a Railway persistent volume to survive redeploys.
# On Railway: attach a volume at /data, then set DATABASE_PATH=/data/okinawa.db
# Leave empty in development to use ./okinawa.db
DATABASE_PATH=
